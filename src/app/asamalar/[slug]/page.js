// Minimal reproduction for debugging - JS Component
export default function StagePage({ params }) {
    return (
        <div>
            <h1>Debug Page (JS)</h1>
            <p>Minimal sync component in JS.</p>
        </div>
    );
}
